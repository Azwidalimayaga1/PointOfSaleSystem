# Database
POS_DB_HOST=localhost
POS_DB_NAME=pos_system
POS_DB_USER=root
POS_DB_PASS=

# Firebase Authentication > Project settings > General > Web API Key
# Restrict this key to the POS application's approved domains in Google Cloud.
FIREBASE_WEB_API_KEY=replace-with-your-firebase-web-api-key
